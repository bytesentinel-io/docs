import { Tab, Tabs } from 'nextra-theme-docs'

# Installation
 
<Tabs items={['Windows', 'Linux', 'macOS']}>
    <Tab>
        ```shell
        pip install dynami
        ```
    </Tab>
    <Tab>
        ```shell
        pip install dynami
        ```
    </Tab>
    <Tab>
        ```shell
        pip install --user dynami
        ```
    </Tab>
</Tabs>